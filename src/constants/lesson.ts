// Default length for focused subject practice sessions
export const DEFAULT_TRAINING_MINUTES = 20;
export const DEFAULT_TRAINING_SECONDS = DEFAULT_TRAINING_MINUTES * 60;

// Default length for the daily learning universe spanning multiple subjects
export const DEFAULT_DAILY_UNIVERSE_MINUTES = 150; // roughly 2-3 hours
export const DEFAULT_DAILY_UNIVERSE_SECONDS =
  DEFAULT_DAILY_UNIVERSE_MINUTES * 60;

